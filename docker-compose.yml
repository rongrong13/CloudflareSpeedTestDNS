version: '3.8'
services:
  cfstd:
    build: .
    image: cloudflare-speed-localtest:latest
    container_name: cfstd
    restart: unless-stopped
    network_mode: host
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
